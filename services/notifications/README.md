# Notification services

Local notifications after lab sync and related helpers.

**Current files:** `hooks/notifications.ts`, `hooks/useNotificationEngine.ts`.

Notification content and triggers should remain unchanged when moving files; test on a development build where notifications are supported.
