# Storage services

AsyncStorage: team profile, theme preference, per-activity reflection histories.

**Current file:** `hooks/storage.js` (`saveTeamData`, `getTeamData`, `save*Results`, `get*Results`).

**Constraints:** Storage keys and JSON payload shapes must stay backward-compatible for existing devices.
