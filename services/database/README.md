# Database services

Local SQLite access for lab trials (map pins, home stats).

**Current file:** `hooks/database.ts` (`stemmlab.db`, `trials` table).

**Constraints:** Schema and `insertTrial` behaviour are frozen unless a migration is explicitly planned and tested on iOS and Android.
