# Scoring utilities

Ranking and measurement conversion logic.

**Implemented (Stage 2A):**

- `leaderboard-scoring.ts` — overall points, per-team dedupe, activity metric labels

Import via `@/utils/scoring/leaderboard-scoring` (legacy `hooks/leaderboard-scoring` re-export removed).

Sound metering lives in `utils/calculations/sound-metering.ts`.
