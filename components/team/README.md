# Team components

Team profile–specific UI sections.

**Planned contents (Stage 3):**

- Extracted blocks from `app/(tabs)/team.tsx` where it improves readability
- Team-facing sections that are not generic enough for `components/ui/`

**Current location:** `components/ui/lab-journal-section.tsx`, `components/ui/info-row.tsx`, and most of `app/(tabs)/team.tsx`.

Lab journal logic: `utils/formatters/lab-journal.ts` (UI: `components/ui/lab-journal-section.tsx`).
